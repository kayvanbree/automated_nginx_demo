version: '3'

services:
  automated-nginx-demo:
    image: scubakay/automated_nginx_demo:latest
    ports:
      - '3000:80'
    environment:
      VIRTUAL_HOST: 136.144.216.182
      VIRTUAL_PORT: 3000
    restart: unless-stopped

networks:
  default:
    external:
      name: nginx-proxy
