version: '3'

services:
  automated-nginx-demo:
    image: scubakay/automated_nginx_demo:latest
    environment:
      VIRTUAL_HOST: demo.dev.scubakay.com
    restart: unless-stopped

  automated-nginx-demo2:
    image: scubakay/automated_nginx_demo:latest
    environment:
      VIRTUAL_HOST: dev.scubakay.com
    restart: unless-stopped

networks:
  default:
    external:
      name: nginx-proxy
